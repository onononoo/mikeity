"""
the builder: a small static site generator for the history of the world.

it reads json data from data/ and era text from content/, and writes plain
html pages, a search index, and a sqlite database into site/.
"""
